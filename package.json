{
  "name": "amm-dex-monorepo",
  "version": "1.0.0",
  "description": "AMM DEX - Automated Market Maker Decentralized Exchange",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "prettier --write .",
    "clean": "pnpm -r clean",
    "contract": "pnpm --filter @amm-dex/contract"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
